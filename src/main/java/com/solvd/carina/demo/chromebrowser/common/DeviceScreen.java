package com.solvd.carina.demo.chromebrowser.common;

import org.openqa.selenium.WebDriver;

public abstract class DeviceScreen extends ScreenBase {
    public DeviceScreen(WebDriver driver) {
        super(driver);
    }
}
